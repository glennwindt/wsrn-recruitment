// src/training/TrainingScheduler.js
export function scheduleTraining(seafarer, center) {
  // Placeholder function — to be enhanced with routing and availability logic
  return {
    seafarerId: seafarer.id,
    centerId: center.id,
    status: 'scheduled'
  };
}

